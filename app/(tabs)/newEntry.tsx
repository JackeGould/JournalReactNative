import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const NewEntry = () => {
  return (
    <View>
      <Text>NewEntry</Text>
    </View>
  )
}

export default NewEntry

const styles = StyleSheet.create({})

// new entry requires user info, a unique entry id, date/time, as well as the content of the text inputN